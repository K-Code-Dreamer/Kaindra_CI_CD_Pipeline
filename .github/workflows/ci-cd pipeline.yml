name: .NET Framework CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v1.0.0

      - name: Restore NuGet packages
        run: nuget restore

      - name: Build .NET Framework project
        run: msbuild Kiandra_FA2_SLD521_ComicBookStore/p:Configuration=Release

  test:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v1.0.0

      - name: Restore NuGet packages
        run: nuget restore

      - name: Run tests
        run: |
          # Add the command to run tests (e.g., MSTest, NUnit, xUnit)

  deploy:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v1.0.0

      - name: Restore NuGet packages
        run: nuget restore

      - name: Deploy to staging
        run: |
          # Replace this with your deployment script
          # Example: deploying to a staging server using PowerShell script
          .\deploy.ps1 # Assuming you have a PowerShell deployment script

# You can add more jobs here for additional stages (e.g., production deployment)







