name: Kiandra CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v1
        with:
          msbuild-version: 'latest'

      - name: Build application
        run: msbuild /p:Configuration=Release C:\Users\paday\Downloads\Github Repos\Kaindra_CI_CD_Pipeline\Kiandra_FA2_SLD521_ComicBookStore\Kiandra_FA2_SLD521_ComicBookStore.sln
        

  test:
    needs: build
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v1
        with:
          msbuild-version: 'latest'

      - name: Build application
        run: msbuild /p:Configuration=Release C:\Users\paday\Downloads\Github Repos\Kaindra_CI_CD_Pipeline\Kiandra_FA2_SLD521_ComicBookStore\Kiandra_FA2_SLD521_ComicBookStore.sln

      - name: Run tests (replace with actual test commands)
        run: echo "Running tests..."

  deploy:
    needs: test
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v1
        with:
          msbuild-version: 'latest'

      - name: Build application
        run: msbuild /p:Configuration=Release C:\Users\paday\Downloads\Github Repos\Kaindra_CI_CD_Pipeline\Kiandra_FA2_SLD521_ComicBookStore\Kiandra_FA2_SLD521_ComicBookStore.sln

      - name: Deploy to staging (replace with actual deployment steps)
        run: echo "Deploying to staging..."










